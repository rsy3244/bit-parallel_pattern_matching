#include <bits/stdc++.h>

#define ALL(a) (a).begin(), (a).end()

using namespace std;

class T {
	public:
	vector<int> a[3];
	using tmp = a[0];
};

signed main(){
	T a();
	a.tmp.push_back(1);

	return 0;
}

